# DemoGain_GUIExtensions

Demonstrates native and third-party GUI framework integration: JUCE, VSTGUI, Cocoa (native and separate bundle), and Win32. Uses PLUGGUI compile-time flag to switch frameworks while sharing DemoGain's DSP algorithm and parameter model.
