# DemoGain_RelatedType_Original

Source plugin for DemoGain_RelatedType version conversion workflow. Implements custom chunk persistence (`GetChunk`, `SetChunk`, `CompareActiveChunk`) with unique Effect ID and Product ID.
