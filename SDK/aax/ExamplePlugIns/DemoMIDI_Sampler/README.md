# DemoMIDI_Sampler

Drum machine sampler with sample-accurate global/local MIDI processing and `AAX_ITransport` integration for tempo-synced playback. Uses `AAX_CMonolithicParameters` for Native-only processing of embedded samples (bass, snare, tom) triggered by MIDI and transport state.
