# DemoDelay_HostProcessor

Demonstrates the optional AAX Host Processor for advanced offline processing with random access to source audio. Includes two Effects: one with a simple offline delay line, and another that compensates for its delay when rendering to the timeline by using lookahead audio access and priming the delay buffer.
