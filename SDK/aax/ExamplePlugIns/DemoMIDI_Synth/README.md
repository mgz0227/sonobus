# DemoMIDI_Synth

Basic synthesizer using `AAX_CMonolithicParameters` with external state management (`CSimpleSynth` class). Processes audio entirely in the Data Model without a DSP algorithm component, suitable for cross-format frameworks. AAX Native only.
