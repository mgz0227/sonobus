# DemoGain_Background

Demonstrates background routine for algorithm processing using a ring buffer to decouple real-time and background processing threads. Uses AAX direct data interface to communicate algorithmic delay from the processing algorithm to the plug-in controller.
