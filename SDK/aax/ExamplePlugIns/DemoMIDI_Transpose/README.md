# DemoMIDI_Transpose

Example of a MIDI Effect. Transposes incoming MIDI notes. Demonstrates `GenerateCoefficients()` override to package parameters into a coefficients structure and `ResetFieldData()` override for initial private data values.
