# DemoGain_RelatedType

Demonstrates plugin version conversion using `AAX_eProperty_Related_Native/DSP_Plugin_List` to upgrade settings from DemoGain_RelatedType_Original. Implements custom chunk persistence (`GetChunk`, `SetChunk`, `CompareActiveChunk`) for backwards-compatible settings migration.
