# DemoDelay

Demonstrates private data management using `AddPrivateData()` for external memory allocation (`CSimpleDelayLine` object) and multiple data input ports (`AddDataInPort()`) to independently control coefficient data and delay length. Shows sample rate-dependent parameter scaling (milliseconds to samples).
