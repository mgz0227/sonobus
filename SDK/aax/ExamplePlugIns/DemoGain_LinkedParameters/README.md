# DemoGain_LinkedParameters

Stereo version of DemoGain demonstrating parameter linking with two modes: synchronized Gain mode (left/right channels linked) and Pan mode (balanced L/R control). Implements proper touch/release behavior for linked parameters during automation and manual control. Demonstrates dynamically changing parameter taper objects.
