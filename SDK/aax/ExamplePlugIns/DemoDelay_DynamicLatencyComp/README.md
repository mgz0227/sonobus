# DemoDelay_DynamicLatencyComp

Demonstrates runtime algorithmic latency changes using `SetSignalLatency()` and handling `AAX_eNotificationEvent_SignalLatencyChanged` notifications. Features a delay compensation toggle that declares variable delay to the host, allowing the algorithm's latency to change dynamically based on the delay parameter while maintaining proper timeline synchronization.
