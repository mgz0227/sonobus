# AAX SDK Unit Tests

This directory contains unit tests for the AAX SDK.

## Overview

The unit tests are built using CMake and use Google Test (gtest) and Google Mock (gmock) frameworks.

To build and run all tests, use the `build_unit_tests.py` script in the Scripts directory.


## Dependencies

- Google Test (gtest) / Google Mock (gmock)
